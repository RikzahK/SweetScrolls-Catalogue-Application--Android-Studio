package com.rikzah.sweetscrolls_rikzah_khattal;

import androidx.appcompat.app.AppCompatActivity;

import android.os.Bundle;

public class categoryitems extends AppCompatActivity {

    // This method is called when the activity is first created
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);

        // Set the content view to the layout defined in activity_categoryitems.xml
        setContentView(R.layout.activity_categoryitems);
    }
}
